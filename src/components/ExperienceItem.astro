---
interface Props {
  company: string;
  tenure?: string;
  roles: Array<{
    title: string;
    period: string;
    location?: string;
    description: string[];
  }>;
}

const { company, tenure, roles } = Astro.props;
---

<div class="fade-in">
  <div class="mb-6 flex items-baseline gap-4">
    <h3 class="font-display text-2xl font-semibold text-foreground">{company}</h3>
    {tenure && (
      <span class="font-mono text-xs uppercase tracking-[0.05em] text-muted">{tenure}</span>
    )}
  </div>
  <div class="flex flex-col gap-8 border-l border-overlay/12 pl-6 md:pl-10">
    {roles.map((role) => (
      <div class="grid gap-2 md:grid-cols-[200px_1fr]">
        <div>
          <p class="font-mono text-xs uppercase tracking-[0.05em] text-muted">{role.period}</p>
          {role.location && (
            <p class="font-mono text-xs text-muted/60">{role.location}</p>
          )}
        </div>
        <div>
          <h4 class="text-base font-semibold text-foreground">{role.title}</h4>
          {role.description.map((p) => (
            <p class="mt-2 text-base leading-relaxed text-muted">{p}</p>
          ))}
        </div>
      </div>
    ))}
  </div>
</div>
