---
import CodeCard from "./CodeCard.astro";
import SectionShell from "./SectionShell.astro";

interface Props {
  entries: Array<{
    id: string;
    title: string;
    type: string;
    description: string;
    tags: string[];
    url?: string;
  }>;
}

const { entries } = Astro.props;
---

<SectionShell
  id="code"
  eyebrow="Code"
  title="Things I've built"
  subtitle="Currently deep in agentic engineering."
  showConnector
>
  <div class="code-list border-t border-foreground">
    {entries.map((entry, i) => (
      <CodeCard
        index={i + 1}
        title={entry.title}
        type={entry.type}
        description={entry.description}
        tags={entry.tags}
        url={entry.url}
      />
    ))}
  </div>
</SectionShell>
