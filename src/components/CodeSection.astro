---
import CodeCard from "./CodeCard.astro";
import SectionShell from "./SectionShell.astro";

interface Props {
  projects: Array<{
    id: string;
    title: string;
    type: string;
    description: string;
    tags: string[];
    url?: string;
  }>;
}

const { projects } = Astro.props;
---

<SectionShell
  id="code"
  eyebrow="Code"
  title="Things I've built"
  subtitle="Currently deep in agentic engineering."
  showConnector
>
  <div class="code-list border-t border-black">
    {projects.map((project, i) => (
      <CodeCard
        index={i + 1}
        title={project.title}
        type={project.type}
        description={project.description}
        tags={project.tags}
        url={project.url}
      />
    ))}
  </div>
</SectionShell>
